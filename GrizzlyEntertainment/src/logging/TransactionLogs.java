package logging;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class TransactionLogs {
	private static final Logger logger = LogManager.getLogger(TransactionLogs.class);
}
