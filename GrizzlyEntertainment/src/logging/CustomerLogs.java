package logging;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class CustomerLogs {
	private static final Logger logger = LogManager.getLogger(CustomerLogs.class);
}
